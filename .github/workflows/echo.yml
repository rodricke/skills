name: hello-world
on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options:
        - monalisa
        - credit
        - one
        default: credit
      message:
        required: true
      use-emoji:
        type: boolean
        description: Include 🎉🤣 emojis
        

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: step-zero
        run: echo "step-zero who"
        env:
          FIRST_NAME: Mona
          middle_name: The
          Last_Name: Octocat
      - name: my-step
        run: echo "Hello World!  ${{github.event.inputs.message}} ${{ secrets[format('BIG_SECRET')] }}" 
      - name: my-step2
        run: |
          echo "Rodrick!"
          echo "second line world $GITHUB_WORKFLOW $Last_Name"
      - name: Send greeting
        run: echo "${{ github.event.inputs.message }} ${{ fromJSON('["", "🥳"]')[github.event.inputs.use-emoji == 'true'] }} ${{ github.event.inputs.name }}"
